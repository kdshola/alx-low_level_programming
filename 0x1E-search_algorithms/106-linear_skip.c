#include "search_algos.h"

/**
 * linear_skip - function that searches for a value in a
 * sorted skip list of integers.
 * @list: pointer to the first node of the skip list to search in
 * @value: the value to search for
 * Return: the pointer to the first node where value is located or NULL
 */
skiplist_t *linear_skip(skiplist_t *list, int value);
{
}
